// Program to compare two numbers and print the larger one

// Initialize two numeric variables
let a = 20;
let b = 30;

// Compare the two numbers
if (a > b) {
    // If a is greater than b, print A is biggest
    console.log("A is biggest");
} else {
    // If b is greater than or equal to a, print B is biggest
    console.log("B is biggest");
}
